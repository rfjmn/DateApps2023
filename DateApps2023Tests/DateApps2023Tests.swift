import XCTest
@testable import DateApps2023

final class DateApps2023Tests: XCTestCase {

    override func setUpWithError() throws {
    }

    override func tearDownWithError() throws {
    }

    /// テスト追加用の空のテンプレート。機能の正しさはまだ検証しません。

    func testExample() throws {
    }

    /// 計測対象が未設定のテンプレート。性能の回帰検証には未使用です。

    func testPerformanceExample() throws {
        self.measure {
        }
    }

}
